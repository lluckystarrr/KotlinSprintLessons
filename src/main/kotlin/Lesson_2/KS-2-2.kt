package org.example.Lesson_2

fun main() {

    val workers = 50
    var workersSalary = 30000
    val intern = 30
    val internsSalary = 20000

println(workersSalary * workers)
    println((workersSalary * workers) + (internsSalary * intern))
    println((workersSalary + internsSalary)/2)


}
